(define (p) (p))
(define (test x y)
  (if (= x 0) 0 y))

(test 0 (p))
; 適用順序評価 (元の本では「作用的順序」となっているらしい)
; => 無限ループに入る。
; 適用順序評価を使うインタプリタでは先に式の引数の部分を評価していく。
; (test 0 (p))の(p)を評価しようとして無限ループに入る。

; 正規順序評価
; => 0が返る。
; 正規順序評価を使うインタプリタでは引数の評価の前に式を展開する
; (if (= 0 0) 0 (p))
; 引数は最初に評価しないので、(p)はまだ評価されていない。
; if文が評価されると、(= 0 0) が true なので (p)は評価されずに処理が終了する。
; https://nasada13.blogspot.com/2017/10/sicp-105.html
